{
  "name": "pixel-critter-dash",
  "version": "0.1.0",
  "description": "Online party multiplayer pixel animal endless runner with room codes.",
  "type": "module",
  "scripts": {
    "start": "node server/server.js",
    "dev": "node server/server.js",
    "check": "node --check server/server.js && node --check tests/smoke.js",
    "test": "node tests/smoke.js"
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=20"
  }
}
